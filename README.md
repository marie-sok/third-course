# third-course

Small Maven/Java study project.

## Current setup

- Maven
- Java 23
- artifact: `org.skypro:Hogwarts`
- entry point: `src/main/java/org/skypro/Main.java`

## Build

```bash
mvn clean compile
```

IDE metadata is intentionally excluded from the repository; local `.idea/` files should stay untracked.
